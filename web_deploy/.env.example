# Frontend Configuration (Client-Side)
# URL for the proxy or direct backend connection
VITE_API_URL=https://voltcore-node.hf.space/api/rpc

# Backend Configuration (Server-Side Proxy)
# The actual IP/URL of the Volt Core node
VOLT_NODE_URL=http://volt-core.zapto.org:6001
